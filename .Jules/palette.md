## 2024-05-22 - Missing ARIA Labels on Icon Buttons
**Learning:** The builder interface relies heavily on icon-only buttons without accessible labels, making it difficult for screen reader users to navigate the toolbar and sidebar.
**Action:** Always verify icon-only buttons have `aria-label` or `aria-labelledby` during the observation phase.
