## 2026-01-22 - Accessibility in Builder Layouts
**Learning:** Icon-only buttons and custom tab implementations are common accessibility gaps in builder interfaces. Simple ARIA labels and roles significantly improve navigability without design changes.
**Action:** Always check `Sidebar` and `Toolbar` components for missing ARIA attributes on icon buttons.
